{{ dump(form2) }}
{#{{ dump(list) }}#}
{{ form_start(form2) }}
    {{ form_row(form2.name) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form2) }}

<h1>Liste des catégories</h1>
{% for category in list %}
<ul>
    <li>{{ category.name }}</li>
</ul>

{% endfor %}