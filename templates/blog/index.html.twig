{# templates/blog/show.html.twig #}
{% extends 'base.html.twig' %}

{#{% block title %}{{ slug }}{% endblock %}#}

{% block body %}
    {#{{ dump(articles) }}#}
        <div class="form-group bg-warning col-8">
            <h3>Ajouter un article</h3>
            {{ form_start(form2) }}
            <h3>Titre</h3>
            {{ form_widget(form2.title, {'attr':{'class':'form-control'}}) }}
            <h3>Contenu</h3>
            {{ form_widget(form2.content, {'attr':{'class':'form-control'}}) }}
                {#{{ form_widget(form2.category) }}#}
            <h3>Catégorie</h3>
            {{ form_widget(form2.category, {'attr':{'class':'form-control'}}) }}
            <button class="btn">{{ button_label|default('Save') }}</button>
            {{ form_end(form2) }}
            {#{{ form_widget(form2) }}#}
        </div>


    {% for toto in articles %}
        <div class="row bg-info m-3 shadow">
            <div class="col-4">
                    <img class="img-fluid rounded m-4 shadow" src={%  if toto.picture == true %} "/{{ toto.picture }}" {% else %} "https://via.placeholder.com/500" {% endif%} alt="Card image cap">
                    {#<img class="img-fluid rounded m-4 shadow" src="/{{ toto.picture}}" alt="Card image cap">#}
            </div>
            <div class="col-7 m-5">
                <h5>{{ toto.title }}</h5>
                <p class="p-3">{{ toto.content }}</p>
                <a href="{{ path('article_show', {'id': toto.id}) }}" class="btn btn-primary p-2 mb-3">En savoir plus</a>
                <ul class="list-unstyled">
                    {% for tag in toto.tags %}
                        <a href="{{ path('tag_show', {'name': tag.name}) }}"><li>#{{ tag.name }} </li></a>
                    {% else %}
                        <li>No tags for this article</li>
                    {% endfor %}
                </ul>
            </div>
        </div>

    {% endfor %}

{% endblock %}


    {#<hr>#}

    {#{{ form_start(form) }}#}
    {#{{ form_row(form.searchField) }}#}
    {#<button type="submit" name="btnSearch">Rechercher</button>#}
    {#{{ form_end(form) }}#}

    {#<hr>#}

    {#<hr>#}

